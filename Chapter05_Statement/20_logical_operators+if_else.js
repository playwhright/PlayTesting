let user = "abc", password = "123", IsAccountLocked = false;

if ((user === "abc" && password === "123") && !IsAccountLocked) { // Use small if (imp)
    console.log("User allow to Enter");
} else {
    console.log("User not allow to Enter");

}
