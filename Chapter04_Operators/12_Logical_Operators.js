let a = true;
let b = false;

console.log(a && b); // AND Operator (both should True then only True)
console.log(a || b); // OR Operator (Atleast one should be True)
console.log(!a);  // Revesre (value of a = true so output = false)