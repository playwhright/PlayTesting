let abc = "Haladkar";
let full_name = `Sandesh Sarjerao ${abc}`; // we can uese one literal (value) for multiple lines with $()
console.log(full_name);

let math = `2+2=${2 + 2}`;
console.log(math);

// To store path (for \\) of the file --- use extra \
let path = "C:\\Users\\Shailesh Haladkar\\OneDrive" //   use double \\
console.log(path);

//To store URL (/) use directly
let url = "https://www.instahyre.com/candidate/activity/";
console.log(url);